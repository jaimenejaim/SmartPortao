<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <group
        android:id="@+id/menu_top">
    <item
        android:id="@+id/nav_home"
        android:icon="@drawable/house_purple"
        app:itemIconTint="@color/colorPrimary"
        android:title="Início" />
    </group>

    <group
        android:id="@+id/menu_historic">
        <item
            android:id="@+id/nav_historic"
            app:itemIconTint="@color/colorPrimary"
            android:icon="@drawable/ic_history"
            android:title="Histórico"/>

    </group>

    <group
        android:id="@+id/menu_to">
    <item
        android:id="@+id/nav_settings"
        app:itemIconTint="@color/colorPrimary"
        android:icon="@drawable/ic_settings"
        android:title="Configurações"/>

    </group>
    <group
        android:id="@+id/menu_evaluate"
        >
        <item
            android:id="@+id/nav_evaluate"
            app:itemIconTint="@color/colorPrimary"
            android:icon="@drawable/ic_like"
            android:title="Avaliar" />
    </group>

    <group
        android:id="@+id/menu_bott1"
        >
    <item
        android:id="@+id/nav_about"
        app:itemIconTint="@color/colorPrimary"
        android:icon="@drawable/ic_help"
        android:title="Sobre" />
    </group>

    <group
        android:id="@+id/menu_limiter"
        >
        <item
            android:title="" />
    </group>

</menu>
